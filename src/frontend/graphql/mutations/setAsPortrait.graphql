mutation SetAsPortrait($id: ID!, $projectId: ID!){
  setPortraitToProject(id: $id, projectId: $projectId){
    id
    name
    portraitId
    images{
      id
      path
    }
    createdAt
    updatedAt
  }
}